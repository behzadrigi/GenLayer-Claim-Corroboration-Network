# GenLayer-Claim-Corroboration-Network
Multi-contract claim verification network on GenLayer. Registers a claim with independent sources, corroborates it through live on-chain fetching and validator consensus, classifies it, and updates the submitter's reputation, all bound to authenticated on-chain state rather than caller-supplied data.
